{
  "name": "carbonflow",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" --kill-others-on-fail",
    "install:all": "npm install --prefix frontend && npm install --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "framer-motion": "^12.38.0",
    "react-is": "^19.2.4"
  }
}
